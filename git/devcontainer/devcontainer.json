{
    "image": "ledanghuy/ubuntu2404:devcontainer",
    "runArgs": [
        "--privileged",
        "--network=harness_default",
        "--restart=always"
    ],
    "postCreateCommand":"sudo update-ca-certificates",
    "mounts":[
        "source=/workspace/certs/host.crt,target=/home/vscode/.local/share/code-server/localhost.crt,type=bind",
        "source=/workspace/certs/host.key,target=/home/vscode/.local/share/code-server/localhost.key,type=bind",
        "source=/usr/local/share/ca-certificates,target=/usr/local/share/ca-certificates,type=bind"
    ]
}